[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "merge-and-rebase"
version = "0.1.0"
authors = [
  {name = "Aniello Panariello", email = "a.panariello4@gmail.com"},
  {name = "Filippo Rinaldi", email = "filippo.rinaldi@unimore.it"}
]
description = "Codebase for model merging and rebasin"
license = {file = "LICENSE"}
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "torch>=2.1",
  "tqdm>=4.66",
  "numpy>=1.24",
  "pyyaml>=6.0.1",
  "open_clip_torch>=2.24.0",
  "torchvision>=0.16",
  "transformers>=4.38",
  "peft>=0.14.0",
  "protobuf>=6.33.5",
  "sentencepiece>=0.2.1",
  "tiktoken>=0.12.0",
]

[project.optional-dependencies]
# Core data stack for your vision benchmarks (HF datasets + PIL + hub)
data = [
  "datasets>=2.16",
  "Pillow>=10.0.0",
  "huggingface-hub>=0.20",
]

test = [
  "pytest>=8.0",
  "pytest-cov>=5.0",
]

gdrive = ["gdown>=5.2.0"]

dev = [
  "ruff>=0.6.0",
]

[tool.uv]
python-preference = "managed"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = [
  "E", "F", "I",   # pycodestyle/pyflakes/isort
  "B",             # flake8-bugbear
  "UP",            # pyupgrade
]
ignore = [
  "E501",          # allow long lines; handled by formatter policy / readability
]

[tool.ruff.lint.isort]
known-first-party = ["merge_and_rebase"]
